name: conversion_by_device
nodes:
  - node1:
      sql: |
        SELECT
          device_type,
          sum(pageviews) AS views,
          sum(conversions) AS total_conversions,
          round(sum(conversions)/sum(pageviews), 3) AS rate
        FROM daily_traffic_metrics
        GROUP BY device_type
publish: conversion_by_device
