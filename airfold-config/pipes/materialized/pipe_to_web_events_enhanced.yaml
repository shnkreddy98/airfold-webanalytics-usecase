nodes:
  - load:
      sql: |
        SELECT
          user_id,
          session_id,
          timestamp,
          page_url,
          page_sequence,
          is_landing_page,
          is_exit_page,
          referrer,
          campaign,
          duration,
          conversion_flag,
          device_type,
          browser,
          city,
          is_new_user,
          session_duration
        FROM web_events
to: web_events_enhanced
