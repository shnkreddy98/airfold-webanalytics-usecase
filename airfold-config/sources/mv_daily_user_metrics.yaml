name: mv_daily_user_metrics
cols:
  user_id: Int32
  date: Date
  session_count: UInt64
  daily_duration: Float64
  conversion_per_day: Float64
settings:
  engine: ReplacingMergeTree()
  order_by: "`date`"
