name: daily_conversion_rate
nodes:
  - node1:
      sql: |
        SELECT
          event_date,
          round(sum(conversions)/sum(pageviews), 3) AS conversion_rate
        FROM daily_traffic_metrics
        GROUP BY event_date
        ORDER BY event_date
publish: daily_conversion_rate
