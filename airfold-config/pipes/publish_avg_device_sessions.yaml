nodes:
  - avg_device_sessions:
      sql: |
        SELECT 
            device_type, 
            AVG(duration) AS avg_session_duration
        FROM web_events
        GROUP BY device_type
        ORDER BY avg_session_duration DESC
publish: avg_device_sessions
